<h1>All Tags</h1>

<ul>
  <% @tags.each do |tag| %>
    <li>
    <% if logged_in? %>


    <%= link_to tag.name, tag_path(tag) %>
    &nbsp
    <%= link_to "destroy", tag_path(tag), :method => :delete  , data: {confirm: "Really delete the tag?"}%>
    <% end %>

    </li>
  <% end %>
</ul>